# Aster — Learned Query Optimization for PostgreSQL

Aster is under active construction. The first verified phase implements PostgreSQL `EXPLAIN (FORMAT JSON)` parsing, structural plan canonicalization/deduplication, graph construction, and provenance records for measured plan observations.

No performance or dataset-size claims are made yet.
